name: csharp

on:
  push:
    branches: [main]
    paths:
      - 'langs/csharp/**'
      - 'vendor/vmx/**'
      - 'spec/**'
      - '.github/workflows/csharp.yml'
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: langs/csharp
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore GuideArch.sln

      - name: Build
        run: dotnet build GuideArch.sln --configuration Release --no-restore

      - name: Test (xUnit)
        run: dotnet test GuideArch.sln --configuration Release --no-build --nologo --verbosity normal

      - name: Format check
        run: dotnet format GuideArch.sln --verify-no-changes --no-restore
